vue项目中，组件化页面，多套组件构成一个页面，当某个组件期望优选dom渲染，比如骨架屏组件

在期望优选dom渲染的组件中，如果有逻辑代码，会按照生命周期的顺序执行，会导致期望优选dom渲染的代码，也会在生命周期代码执行后，再按顺序对应执行
比如骨架屏中图片，期望图片加载后执行逻辑，按照定义onload方法在methods中去绑定到template，会导致onload方法需要等整个页面加载后才执行，晚于预期

此时优选执行渲染的代码，可以直接将onload事件用原生js的写法，直接写在html标签上，不用@load的写法绑在template上，可以不用等待整个页面的生命周期，直接渲染执行