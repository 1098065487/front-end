scale度量，定义数据类型和展示方式，一般和axis结合定义轴线，scale定义轴线的类型，范围，自定义刻度等，axis定义轴线的样式等

1.参数传入有三种：
1）配置项对象作为参数，同时设置多个字段
2）第一个参数为字段名，第二个参数为该字段的配置，仅设置单个字段的度量
3）第三种结合了1）和2），独立的用1），公共的提取到单独的配置中，两个参数

2.重要属性：
1）range  输出绘图范围
2）alias  数据字段别名
3）nice  自动调整min, max
4）formatter  函数自定义坐标轴刻度的展示，也会影响到legend和tooltip