h5存在弹窗滚动穿透问题，即弹窗所在页面可以滚动，当弹窗拉起时，滑动遮罩层，引起底层的页面跟随滚动

本质原因是弹窗也属于页面文档流，当滑动弹窗时，事件向上传递，到可滚动dom时，触发了滚动

解决：
可以在弹窗最外层添加ontouchmove事件，在事件中阻止默认事件，防止向上传递
ontouchmove:  e => e.preventDefault() 

注意：事件只能加载弹窗组件的最外层，如果弹窗内部有滚动，哪怕多套一层，也得放到最外层，避免干扰弹窗内滚动