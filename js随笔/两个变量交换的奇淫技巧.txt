两个变量交换值，有很多种方法：
let a = 1;  
let b = 2;

1.最常见的，通过第三方变量缓存一个值：
let temp = a;
a = b;
b = temp;

有很多场景下，不允许使用第三方变量：
当两个变量为简单的数字时：
2.运算技巧处理：
2.1加法：
a = a + b;
b = a - b;
a = a - b;

2.2异或：
a = a ^ b;
b = a ^ b;
a = a ^ b;

当变量为非数字或者复杂值时：
3.通过数组为引用类型的特性
[a, b] = [b, a];

总结：
一般会通过第三方变量，为数字时，通过异或运算，速度最快，为非数字时，可以使用数组特性