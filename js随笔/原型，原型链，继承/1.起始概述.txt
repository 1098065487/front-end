1.为了更好的实现js的继承概念，引入了原型（prototype）概念，将描述对象的公共属性和方法，放入对象的prototype属性中，这样创建的实例就可以获得这些公共属性和方法，而针对不同实例会有不同的属性和方法，放入构造函数中，在new方法实例化对象时传参，保证不同实例独特性。

2.原型和原型链是JS实现继承的一种模型
   原型链的形成，实际靠的是__proto__，而非prototype，每个对象都有__proto__属性，内容是指向的构造的prototype。