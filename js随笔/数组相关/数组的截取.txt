数组的截取，主推slice()，他不会改变原始数组，如果要求改变原始数组，可以使用splice()，本质是删除，返回值是被删除的数据，也可以看作截取出的数据

1.slice()
两个参数，第一个为开始下标，截取包含，第二个为结束下标，截取不包含

第二个参数不存在，则表示从开始到末尾的全部截取

一个参数都不存在，则表示数据的复制，不干扰原数组

2.splice()
本质是删除或插入，第一个参数为删除开始下标，第二个为删除数量，后续若有参数则为插入替换参数，返回值为删除的数据，也可视为截取的数据，会改变原始数组