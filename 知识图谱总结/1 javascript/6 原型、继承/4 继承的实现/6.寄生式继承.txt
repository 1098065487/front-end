1.原理：
在原型式继承的基础上，再封装一层，增强返回的对象，可以理解成继承后再拓展一些
function create() {
  var clone = Object.create(original);
  clone.fn = function () {};
  return clone;
}

2.优缺点：
由于只是在原型式继承的基础上，拓展了返回对象，其优缺点和原型式继承一样

再有就是，增强对象时，如果增强了新的方法，在实例对象时，会和构造继承一样，每次实例都会创建一次增强方法，造成资源浪费