受浏览器同源策略限制，前后端通信会产生跨域问题，解决跨域问题，一般使用CORS跨域资源共享解决，目前浏览器大多实现了CORS，（浏览器限制可以理解成跨域请求可以发起，但是返回结果被拦截，仅限理解），所以针对跨域问题，目前一般是由服务端去实现CORS接口，即可解决

使用CORS标准，针对非简单请求，会先发出一个预检请求，options，从而获知服务端是否允许该跨域请求

GET，POST，HEAD属于简单请求
PUT，DELETE，CONNECT，OPTIONS，TRACE，PATCH会触发预检请求