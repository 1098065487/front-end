当定义了多个同名的函数、接口或类时，会合并成一个类型
（其实就是简单的相加理解，既然定义的是同一个名字，使用类型时，肯定都满足，看作一个嘛）

1.函数合并
之前基础里面的函数重载的形式，其实在引用类型时，就是合并成一个在使用

2.接口合并：
1）接口中属性合并必须类型是一致的（一致才能保证合并不出错，不然会报错的）
2）接口内方法合并和函数合并一样，就像函数重载一样，都被相加合并

3.类的合并和接口一致（都是分属性和方法）

这里注意一点，接口和类的定义，在ts里面看类型的话，都是仅是类型的定义，在使用时，记得约束类型后，还得再实现或者赋值。