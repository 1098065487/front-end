HTTP是一种通讯协议，是在web上进行数据交换的基础

常规流程：客户端发出请求request，服务端返回响应response，其中很多过程被隐藏，这里主要关注http协议

http是无状态有会话的，同一个连接中，两个成功的请求之间没有关系，一般交互的联系会添加http cookies，创建会话让每次请求共享上下文信息，达成相同的状态

http流：
1.打开一个tcp连接，客户端可以打开新的连接，或者重用已经存在的连接
2.发送一个http报文
3.读取服务端返回的报文信息
4.关闭连接或者为后续请求重用连接